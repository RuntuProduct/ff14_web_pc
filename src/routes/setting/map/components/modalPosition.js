import React from 'react'
import PropTypes from 'prop-types'
import {  } from 'antd'
import les from './modalPosition.less'

const ModalPositionCon = ({
  data,
}) => {
  const { img } = data
  return (
    <div>aa</div>
  )
}

ModalPositionCon.defaultProps = {
  // dealMenuClick: false,
}

ModalPositionCon.propTypes = {
  // dealMenuClick: PropTypes.bool,
}

export default ModalPositionCon
